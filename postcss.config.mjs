/**
 * POSTCSS CONFIG - IL PONTE DEFINITIVO (V2)
 * MENTORE DOCET: 
 * 1. Questo file DEVE ESSERE L'UNICO file PostCSS nella root.
 * 2. Se hai un 'postcss.config.js' (con estensione .js), CANCELLALO SUBITO.
 * 3. Se hai un 'tailwind.config.js', CANCELLALO pure quello per ora.
 * * POSIZIONE: Cartella principale (Root).
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
